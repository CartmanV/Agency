# CLAUDE.md — сайт направления «Агентства»

> Перед любой работой прочитай ТЗ:
> 1. **@../для сайта/00 — Архитектура и карта блоков.md** — актуальная карта блоков, нумерация итераций v3, навигация, зафиксированные решения (раздел 5a). Это новый верхний источник истины.
> 2. **@../для сайта/01..11** — по одному детальному ТЗ на каждый блок/страницу (бэклог, must, дерево, видение, исследования, агентства, метрики, легенды, лестница, рынок, карта L1/L2).
> 3. @../Сайт направления — архитектура и план.md — базовая архитектура («данные отдельно от представления», поток сборки, git-процесс правок раздел 9). Остаётся в силе; папка `для сайта/` — дельта поверх него.

Статический сайт-просмотрщик по направлению. Данные отдельно от представления:
HTML/JS не хранят чисел, читают `data/*.json`, которые собирает `build/build.py`
из исходников в родительской папке `My work`.

## Поток данных
```
../*v6*.xlsx  ──[build/build.py]──►  data/backlog.json  ──►  HTML/CSS/JS
```
Изменился источник → `python3 build/build.py` → коммит → (позже) GitHub Pages.

## Команды
- Сборка данных: `python3 build/build.py` (нужен `openpyxl`).
- Локальный просмотр: `python3 -m http.server` → http://localhost:8000
  (через `file://` fetch JSON не работает — только http).

## Конвенции
- **id итерации**: `<тема>-<уровень2>-<#>` в ascii-slug, повтор тема==L2 схлопывается
  (напр. `zakazy-1`, `predlozheniya-2-0-214`). `#` уникален → id стабилен.
- RICE = `round(Reach*Impact*Confidence/Effort)`; Final = `round(RICE*PVMult)`.
  В xlsx это формулы без кеша — считаем в `build.py`, не берём из ячеек.
- `cardPath` = null до итерации 3 (гибрид карточек L3).

## Запреты
- Не хардкодить цифры в HTML — только через `data/*.json`.
- Не плодить метрики сверх 9 активных + 6 целевых.
- Не цитировать неподтверждённые доли (напр. 69.7% оффлайна — не baseline).

## Процесс правок (ТЗ, раздел 9)
- Правка **данных** (бэклог/цифры/тексты): меняем источник в `My work` →
  `python3 build/build.py` → коммит. HTML не трогаем.
- Правка **представления**: отдельная git-ветка под одну правку, маленькие коммиты,
  проверка локально → мерж в `main`. `main` всегда рабочая.
- Не редактировать закоммиченную итерацию «поверх» — новая правка = новое изменение в ветке.
- Перед мержем показать `git diff`: убедиться, что изменилась только нужная страница.
- Общие `assets/app.js` и `assets/style.css` — единственное «общее» место риска:
  меняешь их → проверяешь все страницы.
- **Проверка «глазами новичка» (обязательно после сборки результата).** Перед коммитом
  перечитай весь видимый текст страницы от лица человека, который впервые видит сайт и не
  знает контекста направления. Всё ли он поймёт? Убери или расшифруй простым русским:
  английские слова и кальки (adoption, renewal, Big job, JTBD, SLA, CSAT/NPS…), внутренний
  жаргон (терминал, паритет, ЛПР, дашборд, Q2, Тип 2, АА…), сокращения (допущ.) и абстрактные
  формулировки. Ориентир — простота объяснения, а не полнота терминологии.

## Нумерация итераций — КАНОН v3 (2026-06-04)

Рабочая нумерация переосмыслена. Старый план (`Сайт…план.md` разд.7) и код разошлись — держим v3:

| Новый № | Блок | Было в коде |
|---|---|---|
| Итерация 0 | Каркас + данные | = осталось |
| **Итерация 1** | **Единый бэклог** (полная информативная страница) | была «бэклог-инструмент» |
| **Итерация 2** | **Must-задачи** (5 ProductV-карточек) | была вынесена отдельно |
| **Итерация 3** | **Дерево работ** — чистое **JTBD** (Big→Medium→Small из SVG) | была «Итерация 2: Тема→L2→L3» |

Важно: старое дерево (Тема→L2→Итерация из бэклога) **перестаёт быть `tree.html`** — оно
переезжает на новую страницу **«Карта уровней L1/L2»** (ТЗ 11), переиспользуя текущий код.
`tree.html` перепроектируется в JTBD-дерево по `../Дерево работ — диаграмма.svg` (онтология
`../Дерево работ направления «Агентства».md` v1.2).

## Карта страниц (актуально 2026-06-10)

Итерации (ядро): `backlog.html` (1) · `must.html` (2) · `tree.html` JTBD (3).
Вкладки: Видение (портал) · Исследования · Агентства.Цифры · Метрики · Легенды.
«Карта связей» (2 проекции + реестр) — в vision#karta.

**Слияние «Путь + Концепции» → `etapy.html` (2026-06-10).** Бывшие `lestnica.html` («Путь»)
и `concepts.html` («Концепции») объединены в одну страницу-проекцию **«Этапы ценности»**
(`etapy.html`), источник — авторский `data/etapy.json` из «Этапы ценности направления
«Агентства».md». Концепции понижены из отдельной проекции в слой над этапами (таблица 4
концепций в общей информации + бейдж концепции у каждого этапа). Старые файлы и рендерер
`mountLadder` удалены; новый рендерер — `mountEtapy`. `data/ladder.json` оставлен — его всё
ещё читает `mountLevels` для текстов гипотез в карточках Каталога.

**Модель «2 проекции + реестр» (2026-06-10, было «3 проекции» с 2026-06-06):**
- 2 проекции одного направления (отвечают на разные вопросы):
  `etapy.html` (зачем/когда + что строим) · `tree.html` (чья работа, JTBD).
- Реестр работ (где лежат задачи): `backlog.html` (плоский) · `levels.html` (по темам).
- **Единая шапка `mountProjbar`** (`[data-projbar="etapy|tree|levels"]`) — плашка на 3 страницах:
  проекция/реестр · на какой вопрос отвечает · как читать · чипы на соседние проекции +
  «Карта связей» + «Сокращения ⓘ». Конфиг `PROJ`/`PROJ_SIBS` в app.js.
- **Структура `etapy.html`:** вверху «общая информация» (призма 5 этапов, таблица 4 концепций
  с якорями `#c0..#c3`, 7 подцелей, «куда ведёт», сворачиваемые «как читать гипотезу / логика
  связей / карта документов»); ниже 5 этапов на `<details>` (id `#stage-1..#stage-5`, этапы
  1–2 открыты по умолчанию), каждый — фаза/цель/подцели/гипотезы (карточка → раскрытие)/
  механизмы/задачи/исследования/метрики; затем сквозной слой (H-Отток, И0, карта исследований),
  оговорки, открытые вопросы.

**Единый термин + расшифровка жаргона (2026-06-06, Р4/Р6):**
- Канон названия под-этапов — **«этап 2A / 2B»** везде (в ladder.json было «Блок 2A/2B»;
  в concepts.json — «блок 2A/2B»). `s2b`/`s1` — это CSS-классы, не видимый текст.
- Спрятан сырой жаргон: `[*]` → `<abbr class="assume-mark">*</abbr>` (тултип «редакторское
  допущение»), «бакет» → «группа бэклога», убрана утечка «внутр. классификация Влада» и
  «у Влада» из levels.json, orphan-ветка «Вне карты» → «Вне каталога».
- **Инлайн-глоссарий `gloss()`** (Р6): оборачивает H-коды, E-находки, TTFO/T-op/T-wait/OPEX/
  NSM/Q2 в `<abbr class="gloss" title="…">`. Применяется в рендерерах Лестницы и Концепций;
  для статичного дерева и панели каталога — `glossifyDOM(root)` (TreeWalker по text-нодам,
  пропускает A/ABBR/BUTTON/…). Термины и определения — `GLOSS_TERMS` в app.js.

**Обзор-первой + Start here + группировка NAV (2026-06-06, Р5/Р7/Р9):**
- Дерево (`tree.html`): 3 Big-блока **свёрнуты по умолчанию** — обзор из 3 ролей, детали по
  клику. Раскрытие — кнопкой «Раскрыть всё» в тулбаре.
- Главная (`index.html`): блок **«Маршрут чтения за 15 минут»** — 4 шага по проекциям +
  реестр (1 Видение → 2 Лестница → 3 Концепции → 4 Дерево → реестр). Стили `.sh-list/.sh`.
- NAV сгруппирован: **Обзор · Проекции · Работа · Данные · Справка** (поле `group` в `NAV`,
  `mountHeader` вставляет `<span class="nav__glabel">`). На ≤980px метки группы скрыты, чтобы
  не ломать компактный режим хедера.

**Канон визуального кодирования (2026-06-06, Р8):**
- **Этапы** — единая палитра: `1 = pink` (вход/доверие), `2A/2B = emerald` (фокус Q2),
  `3/4/5 = muted с пунктирной границей` (вне Q2). Канонический класс `.stage-tag` с
  модификаторами `.s-1 .s-2a .s-2b .s-3 .s-4 .s-5`; JS-хелперы `stageCode(s)` (извлекает
  код из «этап 2A (+2B)»/«4 «Увеличить маржу»») и `stageTag(s)` (рендерит чип). Применяется
  в Каталоге (panel L2 + строки подтем). `.med-stage` (Дерево) синхронизирован: дефолт →
  emerald (был copper), `sx` → пунктирный приглушённый. `.vm-sn` в матрице получил
  `data-stage` и красится в семантический цвет.
- **Роли** — `.role-tag` с `.r-c` (консультант, blue), `.r-s` (супервизор, orange),
  `.r-r` (руководитель/IT/бухгалтерия, emerald) — те же цвета, что в `.dot/.vm-dot`.
- **Полоска-легенда `.color-key`** на главной — единая шпаргалка по палитре, выучивается
  один раз.

**Главная для C-level (2026-06-06):**
- **Executive Summary** (`mountExec` + `data/exec.json`) — секция в первом скролле:
  острый one-liner, прогресс-бар KR-2 (5/20), бизнес-кейс цепочкой (Макро→Сдвиг→Угроза→Рычаг),
  2 ставки со светофором (`status: in-progress|on-track|at-risk|blocked|not-started`),
  3 риск-карточки (`level: high|medium|low`), плашка-запрос руководителю (placeholder).
  `data/exec.json` — авторский, обновляется руками как `now.json`.
- **Hero one-liner** переписан под C-level: «Удержать клиента у агентства до того, как он
  уйдёт напрямую к SmartWay».
- **Режим главной** «Кратко / Полно» (`mountViewToggle`): toggle в верху главной,
  localStorage `agency-view-mode-v1`, состояние — класс `body.mode-brief`. Класс
  `.brief-hide` на «Маршрут чтения 15 минут» и «Разделы для команды».
- **«Куда дальше»** для C-level (Видение · Лестница · Концепции · Сейчас в работе) —
  всегда видна; технические разделы (Бэклог · Must · Дерево · Каталог · Исследования ·
  Метрики · Рынок · Агентства · Легенды) — под `.brief-hide`. Подзаголовки в плитках
  (`<span class="tile__sub">`) переводят жаргон: «Лестница — стратегия по этапам»,
  «Концепции — продуктовые ставки», «Дерево — работа по ролям», «Каталог — оглавление
  над бэклогом».
Страницы-провалы: Лестница ценности · Рынок (`rynok.html`) · Карта уровней L1/L2.
Витрина: `now.html` «Сейчас в работе» (ТЗ 12) — срез разработки для стейкхолдеров.
Навигация — двухуровневый хедер (см. ТЗ 00, раздел 4). NAV = 13 пунктов (now.html —
2-м после «Направление»); на ≤720px компактный режим (`@media` в style.css у `.nav`).

## Зафиксированные решения (2026-06-04) — см. ТЗ 00 разд.5a
- **Бэклог**: полный вид как в xlsx; чекбоксом скрываются 5 второстепенных колонок
  (Статус · ID гитлаб · Проблема:источник · PV Notes · Обоснование), не прятать в раскрытие
  по умолчанию. Сортировка MoSCoW→Final. Раскрытие строки = доп., не замена колонок.
- **Дерево**: чистое JTBD; иерархия тем → страница «Карта L1/L2».
- **Видение**: пересобрать в портал (герой + карточки-входы + мини-схема 2 ставок),
  полный текст — сворачиваемые секции.
- **Исследования**: группировка (Тема/Роль) + раскрытие; цитаты дословно, но обезличенно.
- **Агентства**: срез 30.04.2026 + сегменты (без помесячных графиков на старте);
  отток — отдельным блоком (KR-2 = чистая дельта).
- **Лестница**: источник value ladder **v2.5**.
- **Рынок/20 агентств**: отдельная сессия позже → на сайте placeholder, не блокирует.
- **Карта L1/L2**: самостоятельная страница, переиспользует код старого `tree.html`,
  допущения помечать `[*]`.

## Состояние (что уже построено в коде)
- **Каркас (Итерация 0)**: build.py → `backlog.json` (214 итераций), `index.html`,
  общий хедер, `style.css`, таблица бэклога.
- **Итерация 1 v3 — Единый бэклог (готово)**: полный вид всех 30 колонок как в xlsx
  (горизонтальный скролл, липкий заголовок + липкая колонка раскрытия). 5 колонок
  выключены по умолчанию (Статус · ID гитлаб · Проблема:источник · PV Notes ·
  Обоснование), возвращаются чекбоксами «Колонки» (состояние в localStorage).
  Дефолт-сортировка MoSCoW→Final (тай-брейк Final↓, затем №). Раскрытие строки (▸)
  = полная карточка из 7 блоков. Фильтры: Тема/MoSCoW/Этап/Механизм/Подцель/Gate +
  новые Гипотеза(H-код) и Концентрация. Длинный текст усечён (clamp 2 строки) +
  полностью в раскрытии. Поиск, сортировка кликом, URL-параметры сохранены.
- **Главная — лендинг**: герой (цель «+20»), 3 акцент-блока (NSM + 2 ставки), лестница,
  боли, сетка входов. Полный текст видения — в `vision.html`. Палитра «Refined Brand
  Green» (фон #0A1F16, акценты pink #FF5C8A, gold #E2B355), все пары WCAG AA.
  → ПЕРЕСОБРАТЬ в портал (см. решения).
- **A11y**: :focus-visible, aria-label, aria-sort, color-scheme, theme-color, skip-link,
  prefers-reduced-motion, tabular-nums.
- **Must (= Итерация 2 v3, готово)**: 5 кураторских ProductV-карточек (ревизия 27→5),
  аккордеон, полная карточка L3 + таблица пяти мыслителей (дельты ±, вердикт
  Proceed/Rework). Данные `data/must.json` — **авторский файл, собран вручную** из
  `../Must-итерации — прогон ProductV (2026-06-03).md` (не из build.py). 27 «исторических»
  Must — в Бэклоге по фильтру `MoSCoW=Must`.
- **Итерация 3 — дерево (готово)**: `tree.html` = чистое **JTBD-дерево** (Big→Medium→Small,
  3 Big job на `<details>`, 9 Medium-подцелей, 24 Small job, находки E.1–E.7, силы
  торможения, легенда ролей, раскрыть/свернуть всё). Контент — редакторский, в HTML
  (как `vision.html`); источник `Дерево работ — JTBD (веб).html` v1.2, перенесён в нашу
  палитру (+ токен `--blue` для роли консультанта). Чипы-ветки Small → `backlog.html?theme=`.
- **`levels.html` — Карта L1/L2 (готово)**: иерархия Тема→L2→Итерация из `data/tree.json`
  (бывший рендерер `tree.html`, `mountLevels`, режимы Кратко/Подробно, переходы
  `?q=`/`?theme=`). Редакторские карточки L1 из «Темы уровня 1v2.md» (`levels.json`) —
  следующий шаг (ТЗ 11).
- Источник данных — **v6 (2026-06-03, Reach v1.5)**: 214 итераций, Must=5 (ревизия 27→5
  уже в xlsx). `find_source()` берёт самый свежий файл бэклога по версии vN.

## Что дальше (порядок работ v3)
1. ✅ Итерация 1 — бэклог доведён до полного информативного вида (ТЗ 01).
2. ✅ Итерация 3 — `tree.html` = JTBD (ТЗ 03) + `levels.html` = Карта L1/L2 (ТЗ 11).
   Остался редакторский слой L1 (`levels.json` из «Темы уровня 1v2.md»).
- **Легенды (08, ядро готово)**: `legend.html` + `data/legend.json` (build.py парсит
  вкладку «Легенда» v6 → 8 секций, 57 определений: этапы/подцели/механизмы/скоринг/
  метрики/поля), TOC с якорями, `:target`-подсветка строки. Редакторский слой
  (H1–H5 полностью, карта ролей, фильтры 12.x, расширенный глоссарий) — следующий шаг.
3. ✅ Вкладки: Легенды (08, ядро) · Агентства (06) · Метрики (07) · Исследования (05).
- **Исследования (05, готово)**: `research.html` + `data/research.json` (build.py парсит
  «Реестр исследований — единая таблица.xlsx», 89 находок; имена людей обезличены —
  скобки из Reach убраны). Группировка Тема/Роль (переключатель) + раскрытие находки
  до данных/JTBD/источника; фильтры Тема/Роль/Этап/Механизм/H/Статус гипотезы; статус-
  бейджи (подтв./частично/конкур./гипотеза). Находка→бэклог по H-коду (`?q=`).
  Матрица соответствия (этап→H→боль→итерация→метрика) и gap-анализ — следующий шаг.
- **Метрики (07, готово)**: `metrics.html` + `data/metrics.json` (авторский из «Метрики
  Агент">..md» v1.5, 9 активных + 6 целевых; этап/подцель/гипотеза/направление, baseline у
  TTFO / Hidden-work / Operations per consultant). Карточки по этапам, «ведущие метрики по
  блокам», плашка «baseline нужен», метрика→бэклог (`?q=<код>`; в поиск добавлены поля
  Активные/Целевые метрики). Формулы и актуальные цифры — следующий шаг.
- **Агентства. Цифры (06, готово)**: `agencies.html` + `data/agencies.json` (build.py
  парсит «Свод по агентствам 2026-05.xlsx», срез 31.05.2026 → 16 активных + агрегаты,
  концентрация ТОП-3 85%/ТОП-5 91%). Сводка-плашки, бар концентрации, сегменты→механизм
  (редакторски), таблица с фильтром сегмент/полоса, блок оттока (9 агентств, KR-2).
  Сегментация — рабочее допущение по объёму+тренду. Приватность критична (выручка/имена).
4. Страницы-провалы: ✅ Лестница (09) · ✅ Видение-портал (04) · ✅ Рынок (10) —
   **полный контент готов** (`rynok.html`, редакторский как vision/tree). Источники:
   `Исследование рынка бизнес-тревела РФ 2026.md` + `Сводка_рынок_БТ_2026.xlsx`.
   10 секций: вывод для Ракеты (3 accent-карты), сводная таблица 19 цифр с бейджами
   достоверности (Подтв./Проверить/Противоречиво — `badge--ok/review/must`), макрофон
   (4 фактора), «рост в ₽ / спад в поездках», две стороны (клиент-оптимизатор + юнит-
   экономика агентства, конфликт E.6), зарубежный контр-тренд (+33% → этапы 4–5),
   сегментация (Новички/Risers★/Зрелые), 5 следствий для бэклога, поправки к вводным,
   источники. Кросс-ссылки в backlog/tree/lestnica/research. В NAV; карточка в видении
   из «скоро» → рабочая ссылка. ⚠-цифры помечены к сверке по данным Ракеты.
- **Лестница ценности (09, готово)**: `lestnica.html` + `data/ladder.json` (авторский из
  «Логика ценности».md v2.5): 5 этапов на `<details>` (1–2 подсвечены «фокус 2026»),
  блоки 2A/2B, 19 гипотез H1–H5 (H-код→бэклог `?q=`), критерии, активные/целевые метрики,
  «что делает Ракета / результат / цель», карта 7 подцелей→этап, методологическая
  оговорка (cross-agency, кейс МБС). Связана из Видения-портала (карточка «Стратегия»).
- **Видение-портал (04, готово)**: `vision.html` пересобран в портал — герой (+20, NSM),
  4 карточки-входа (Стратегия→legend#stages · Рынок=скоро · Цифры→agencies · Работа→backlog),
  мини-схема «две ставки → этапы 1–2 → 7 подцелей» (ссылки в tree/legend), блок болей
  Q2 со ссылкой в Исследования, полный текст видения в сворачиваемых `<details>`-секциях.
  index.html остаётся домашней сеткой входов.
- **Сейчас в работе (12, готово)**: `now.html` — витрина срез-разработки для стейкхолдеров
  (редакторский инлайн, ведёт Влад вручную, не из xlsx; ТЗ 12). Активная тема «Оффлайн-
  услуги ЖД и Авиа (Оффлайн 4.0)»: мета-чипы (этап 2/механизм/подцель/H2.1 со ссылками),
  блок Проблема (2 боли + Подтверждение + Зачем), 3 сценария As-will (accent-карты),
  таблица 6 подзадач со статус-бейджами (✅ Сделано `badge--ok` / 🔄 Тестирование
  `badge--review` / ⏳ В очереди plain `badge`), связи (Бэклог/Лестница/Дерево/Агентства).
  В NAV 2-м пунктом. На главной: акцент-блок после героя (розовый, статус-пилюли) +
  плитка 1-й в «Разделах». Обновлять вручную: одна-две темы, дату в шапке, статусы.
- **Концепции ценности (готово)**: `concepts.html` + `data/concepts.json` (авторский из
  «Концепции ценности направления „Агентства".md» v1.0). Средний продуктовый слой между
  Лестницей (зачем/когда) и Деревом (чью работу): 4 концепции (0 Не мешать · 1 Постпродажное ·
  2 Операционные тормоза · 3 Новые деньги), у каждой — этапы/подцели/адресат/механизмы (чип
  «в Бэклоге» по теме)/бейдж Q2(focus/partial/out)/эффект. «Карта соответствия» (матрица
  этап→подцель→концепция→механизмы→роль→фокус) рендерится в `vision.html#karta`
  (`[data-value-matrix]`, тот же `mountConcepts`). Кросс-ссылки: лестница (чип концепции у
  каждого этапа + «три проекции» в lede), дерево (баннер «три проекции»), index/vision-портал.
5. Переходы/полировка/публикация.

ТЗ по каждому блоку — в `../для сайта/`. Базовый план — `../Сайт направления — архитектура и план.md`.
